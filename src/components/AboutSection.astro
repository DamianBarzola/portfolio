---
const { translations } = Astro.props;
---

<section id="about">
  <span class="section-title">{translations?.menu.profile}</span>
  <div set:html={translations?.about} />
</section>

<style>
  section {
    font-size: 1.1rem;
  }
  p {
    color: var(--text-secondary);
  }
</style>
